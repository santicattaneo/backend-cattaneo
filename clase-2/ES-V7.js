// operador exponencial **
const valoresBase = [1, 2, 3, 4];
const valoresElevados = valoresBase.map((num, i)=>num ** i);
// array includes
const nombres = ['Isabella', 'Giovanni', 'Delfina']
if(nombres.includes('Giovanni')){
    console.log('Tenemos el nombre')
} else{
    console.log('No tenemos el nombre')
};